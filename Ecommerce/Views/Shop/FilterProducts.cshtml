
@{foreach (var item in Model)
    {

        <!-- Single Product Area -->
        <div class="col-12 col-sm-6 col-md-12 col-xl-6 prod" id="hamper">

            <div class="single-product-wrapper" style="">
                <!-- Product Image -->
                @*<div class="product-img">*@
                <a href="@Url.Action("productdetails","Shop",new {id=item.Id })"> <img style="" src="@Url.Content(item.ImagePath)" alt=""></a>
                <!-- Hover Thumb -->
                @*<img class="hover-img" src="img/product-img/product2.jpg" alt="">*@
                @*</div>*@

                <!-- Product Description -->
                <div class="product-description d-flex align-items-center justify-content-between">
                    <!-- Product Meta Data -->
                    <div class="product-meta-data">
                        <div class="line"></div>
                        <p class="product-price">@item.Price</p>
                        <a href="@Url.Action("productdetails","Shop",new {id = item.Id})"><h5>@item.Title</h5></a>
                        <a href="product-details.html"></a>

                        @*@Html.ActionLink("Addtocart", "Addtocart", new { id = item.Id })*@

                    </div>
                    <!-- Ratings & Cart -->
                    <div class="ratings-cart text-right">
                        <div class="ratings">
                            <i class="fa fa-star" aria-hidden="true"></i>
                            <i class="fa fa-star" aria-hidden="true"></i>
                            <i class="fa fa-star" aria-hidden="true"></i>
                            <i class="fa fa-star" aria-hidden="true"></i>
                            <i class="fa fa-star" aria-hidden="true"></i>
                        </div>
                        <div class="cart">

                            @Html.ActionLink("Add To Cart", "OrderNow", "Shop", new { id = item.Id }, null)
                            @*  @Html.ActionLink("Add To Cart", "Addtocart", "Shop", new { id = item.Id }, null)*@

                             @using (Html.BeginForm("Addtocart", "Shop", FormMethod.Post))
                                {
                                    <div class="row">
                                        <div class="">
                                            <input type="hidden" name="id" value="@item.Id" />

                                        </div>

                                        <div>
                                            <input type="hidden" placeholder="Quantity" max="10" name="qty" value="1" />

                                        </div>

                                    </div>

                                    <button type="submit"><img src="~/Adminstyle/img/core-img/cart.png" alt=""></button>
                                }
                            @*<a href="@Url.Action("Addtocart","Shop", new { id = item.Id})" data-toggle="tooltip" data-placement="left" title="Add to Cart"><img src="~/Adminstyle/img/core-img/cart.png" alt=""></a>*@
                        </div>
                    </div>
                </div>
            </div>

        </div>
    }
}


